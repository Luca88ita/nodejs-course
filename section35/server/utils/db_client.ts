/* import { createRequire } from "https://deno.land/std@0.177.0/node/module.ts";

const require = createRequire(import.meta.url);
const mongoose = require("mongoose");
const MONGODB_URI = `mongodb+srv://${Deno.env.get(
  "MONGODB_USER"
)}:${Deno.env.get(
  "MONGODB_PASSWORD"
)}@cluster0.1h7avzh.mongodb.net/${Deno.env.get(
  "MONGODB_DATABASE"
)}?retryWrites=true&w=majority`;

const connectToDb = async () => await mongoose.connect(MONGODB_URI);

export default connectToDb;
 */
